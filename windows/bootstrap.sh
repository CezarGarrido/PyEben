pacman -S --noconfirm \
    mingw-w64-$(uname -m)-gtk3 \
    mingw-w64-$(uname -m)-gcc \
    mingw-w64-$(uname -m)-python-pip \
    mingw-w64-$(uname -m)-python3-setuptools \
    mingw-w64-$(uname -m)-python3-gobject \
    mingw-w64-$(uname -m)-poppler \
    mingw-w64-$(uname -m)-libjpeg-turbo \
    mingw-w64-$(uname -m)-zlib \
    mingw-w64-$(uname -m)-libtiff \
    mingw-w64-$(uname -m)-freetype \
    mingw-w64-$(uname -m)-lcms2 \
    mingw-w64-$(uname -m)-libwebp \
    mingw-w64-$(uname -m)-openjpeg2 \
    mingw-w64-$(uname -m)-libimagequant \
    mingw-w64-$(uname -m)-libraqm